#include <stdbool.h>

CPlot plot;

int main() {

    int abc;
    abc = ((1==3) && (3>=5));
    while(!(-4 < 1+0)) {
        plot.line(2, 4+5, 5-2, 4*5/9-5*9);
        while(4 < 1+0) {
            plot.line(2, 4+5, 5-2, 4*5/9-5*9);
            plot.line(4, 4-5, 5*2, 4+5-9*5/9);
        }
        plot.line(4, 4-5, 5*2, 4+5-9*5/9);
    }
}

